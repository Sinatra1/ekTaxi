CREATE TABLE jobeet_routes (id BIGINT AUTO_INCREMENT, price decimal precision: 10 scale: 2 NOT NULL, geoa geometry, geob geometry, created_at DATETIME NOT NULL, update_at DATETIME NOT NULL, PRIMARY KEY(id)) ENGINE = INNODB;
